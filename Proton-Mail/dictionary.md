# Dictionary settings

Close Proton Mail app.

Edit the file `~/.config/Proton\ Mail/app/Preferences` and change the value of `"dictionaries": ["en-GB"]` to the desired language code, for example `"dictionaries": ["nl-NL", "en-GB"]` for Dutch and English.
